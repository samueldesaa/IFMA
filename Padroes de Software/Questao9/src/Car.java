class Car {
    String chassis;
    String engine;
    String features;

    @Override
    public String toString() {
        return "Carro{chassi='" + chassis + "', motor='" + engine + "', recursos='" + features + "'}";
    }
}
